/**
 * PromptCompiler design system — portable token + motion layer.
 * Consumed by every surface (extension overlay/popup, future web/IDE adapters).
 * Pure TS, no DOM imports (plan R-ARCH-1); moves into packages/core in Track C.
 */
export * from "./tokens";
export * from "./motion";
